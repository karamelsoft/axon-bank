version: "3.7"

services:
  customers-command:
    image: axondemo/customers-command:latest
    environment:
      SPRING_PROFILES_ACTIVE: swarm
    networks:
      - axon-demo

networks:
  axon-demo:
    external: true
