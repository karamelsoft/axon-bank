version: "3.7"

services:
  accounts-command:
    image: axondemo/accounts-command:latest
    environment:
      SPRING_PROFILES_ACTIVE: swarm
    networks:
      - axon-demo

networks:
  axon-demo:
    external: true
