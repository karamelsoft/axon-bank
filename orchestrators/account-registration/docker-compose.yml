version: "3.7"

services:
  account-registration:
    image: axondemo/account-registration:latest
    environment:
      SPRING_PROFILES_ACTIVE: swarm
    networks:
      - axon-demo

networks:
  axon-demo:
    external: true
