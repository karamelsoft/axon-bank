version: "3.7"

services:
  interfaces-http:
    image: axondemo/interfaces-http:latest
    environment:
      SPRING_PROFILES_ACTIVE: swarm
    networks:
      - axon-demo

networks:
  axon-demo:
    external: true
